import { ThemeConfig } from 'antd'

const theme: ThemeConfig = {
  token: {
    colorPrimary: '#ff6b00'
  },
  components: {
    Layout: {
      siderBg: '#fff'
    },
    Button: {
      defaultColor: '#000'
    }
  }
}

export default theme
